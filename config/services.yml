# Services for PrestaShop 1.7 - 8
# Minimal configuration for legacy controller compatibility
#
# For PS9+, the module automatically loads services.yml.ps9 with full
# Symfony Grid/Form components during installation.

services:
  _defaults:
    public: true
