# =============================================================================
# Configuration install.sh
# =============================================================================
# Copy this file to .env.install and configure it for your environment

# Type: "module" or "theme"
TYPE="module"

# Path to your local PrestaShop installation
PRESTASHOP_PATH="/path/to/prestashop"

# Docker container name for PrestaShop
DOCKER_CONTAINER="prestashop_container"

# Technical name of the module (lowercase, no spaces)
MODULE_NAME="publikomoduleboilerplate"
